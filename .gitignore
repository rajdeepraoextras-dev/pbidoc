# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/

# Local secrets — never commit real API keys (see .env.example)
.env
.env.local

# Local job sandboxes / outputs (NEVER commit customer data or generated docs)
/tmp_jobs/
*.model.json
*.document.json
*.document.md
out/
*.db
*.db-journal
.pbicompass_history.json

# Power BI source files — never commit customer artifacts
*.pbix
*.pbip
# Sample/upload zips dropped in examples/ may wrap a .pbip — never commit them
/examples/*.zip
# (the synthetic test fixture under tests/fixtures/ is committed deliberately)
!tests/fixtures/**

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local tool config (machine-specific, not project source)
.claude/
